$(document).ready(function(){
	
	//获得屏幕高度和宽度
	var SCREEN_H = $(document).height();
	var SCREEN_W = $(document).width();
	
	var BODY_WITH = getBodyWidth();
	
	//递归适配页面
	adapting($(body));
	
	/**
	 * 返回网页的有效区域的宽度
	 */
	function getBodyWidth(){
		return 1000;
	}
	
	function adapting(root){
		
	}
	
})